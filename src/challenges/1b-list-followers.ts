function listFollowers([follower1, follower2, ...rest]: string[]): string {
  // complete function

  //let [follower1, follower2, ...rest] = followerNames;
  // use array destructuring, for practice!
  return `Followed by ${follower1}, ${follower2} and others` ;
}

export default listFollowers;
